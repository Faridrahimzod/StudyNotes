# StudyNotes

![CI](https://github.com/your-username/course-project-Faridrahimzod/actions/workflows/ci.yml/badge.svg)

Безопасное веб-приложение на FastAPI с реализацией лучших практик безопасности.

## 🚀 Основные возможности

- **Аутентификация и авторизация** - безопасная система управления доступом
- **Валидация входных данных** - защита от некорректных и вредоносных данных
- **Защита от SQL-инъекций** - параметризованные запросы через SQLAlchemy
- **Защита от XSS** - экранирование и валидация пользовательского ввода
- **Безопасная работа с файлами** - проверка MIME-типов, лимиты размеров
- **Контейнеризация** - Docker с multi-stage build и non-root пользователем
- **CI/CD** - автоматические проверки безопасности и тестирование

##  Быстрый старт

### Локальная разработка

```bash
# Клонирование репозитория
git clone https://github.com/Faridrahimzod/course-project-Faridrahimzod.git
cd course-project-Faridrahimzod

# Создание виртуального окружения
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt -r requirements-dev.txt

# Запуск приложения
uvicorn app.main:app --reload
```

### Запуск через Docker

```bash
# Сборка и запуск контейнера
docker-compose up --build -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f app
```

## Ритуал перед PR
```bash
ruff --fix .
black .
isort .
pytest -q
pre-commit run --all-files
```

## Тесты
```bash
pytest -q
```

## CI
В репозитории настроен workflow **CI** (GitHub Actions) — required check для `main`.
Badge добавится автоматически после загрузки шаблона в GitHub.

## Контейнеры
```bash
docker build -t secdev-app .
docker run --rm -p 8000:8000 secdev-app
# или
docker compose up --build
```

## Эндпойнты
- `GET /health` → `{"status": "ok"}`
- `POST /items?name=...` — демо-сущность
- `GET /items/{id}`

## Формат ошибок
Все ошибки — JSON-обёртка:
```json
{
  "error": {"code": "not_found", "message": "item not found"}
}
```

См. также: `SECURITY.md`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`.
